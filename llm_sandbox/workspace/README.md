# Workspace

This sandbox workspace is intentionally kept nearly empty in Git.

Use it for temporary artifacts and smoke tests during local runs.
Anything created here should be treated as disposable unless promoted into `llm_sandbox/memory/` or the main codebase.
